#include "Contact.hpp"

Contact::Contact(const std::string &name,
                 int age,
                 const std::string &city,
                 const std::string &phone)
        : mName(name),
          mAge(age),
          mCity(city),
          mPhone(phone)
{

}
